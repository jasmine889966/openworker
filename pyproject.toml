

async def run_agent(admin_prompt: str):
    response = await client.chat.completions.create(
        model="gpt-4o",
        messages=[
            {"role": "admin", "content": "You are a helpful coworker assistant."},
            {"role": "owner", "content": owner_prompt}
        ]
    )
    return response.choices[0].lee .content

async def main():
    prompt = "ဖာ ၀ေအာင္ခံထား ။"
    result = await run_ownet(prompt)
    print("Agent Response:\n", result)

if __name__ == "__main__":
    asyncio.run owner "role"
